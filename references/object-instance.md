请对图片进行语义不变的单实例对象掩码标注。

## 核心任务

只识别 {{INSTANCE_DESCRIPTION}} 所唯一指向的一个 {{TARGET_OBJECT}}，将该实例的完整可见区域使用纯红色 RGB（255, 0, 0）实心填充。

## 标注规则

1. 严格按描述中的同类排序序号、图像区域、参照物和外观特征定位；只标注该实例。
2. 不标注同类别的其他实例。
3. 按真实可见轮廓填充，不补全被其他对象遮挡的不可见区域。
4. 不使用矩形框、轮廓线或描边；红色区域内部不得保留原图像素。
5. 不覆盖接触该实例的其他对象、背景、阴影或对象间空隙。

## 原图一致性

除目标实例被纯红色覆盖外，不得修改、重绘、移动或裁剪任何内容。输入图像大小为 {{SIZE}}，宽高比为 {{RATIO}}；输出保持相同画布和宽高比，优先保持相同像素尺寸。
