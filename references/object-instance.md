请对图片进行语义不变的单实例对象掩码标注。

## 核心任务

只识别 {{INSTANCE_DESCRIPTION}} 所唯一指向的一个 {{TARGET_OBJECT}}，将该实例的完整可见区域使用 {{ANNOTATION_COLOR_NAME}} {{ANNOTATION_COLOR_RGB}} 实心填充。

## 标注规则

1. 严格按描述中的同类排序序号、图像区域、参照物和外观特征定位；只标注该实例。
2. 不标注同类别的其他实例。
3. 按真实可见轮廓填充，不补全被其他对象遮挡的不可见区域。
4. 不使用矩形框、轮廓线或描边；{{ANNOTATION_COLOR_NAME}} 区域内部不得保留原图像素，不得使用渐变、透明度或其它颜色。
5. 不覆盖接触该实例的其他对象、背景、阴影或对象间空隙。
6. 禁止标注下列实例：{{EXCLUDED_INSTANCES}}。若当前目标出现在该清单中，必须保持原图不变，不得填充其任何碎片，也不得跨越遮挡物连接不连续区域。

## 原图一致性

除合格目标实例的可见区域被 {{ANNOTATION_COLOR_NAME}} 覆盖外，不得增加、删除、移动、重绘或改变任何对象；对象数量、形状、朝向、遮挡关系、背景和构图必须保持不变。

输出允许相对输入进行整图等比缩放，但禁止非等比缩放、局部缩放、裁剪、扩图或改变画布比例。等比缩放后，所有对象的相对位置、相对大小、形状、数量和相互关系必须保持不变。

输入图像大小为 {{SIZE}}，宽高比为 {{RATIO}}；输出必须保持相同宽高比，优先保持相同像素尺寸。
